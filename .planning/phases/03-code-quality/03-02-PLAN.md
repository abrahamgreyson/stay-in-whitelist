---
phase: 03-code-quality
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - update_whitelist/config/config_loader.py
  - update_whitelist/updater.py
  - requirements.dev.txt
autonomous: true
requirements:
  - QUAL-03
  - QUAL-05
  - QUAL-06

must_haves:
  truths:
    - "Commented-out dead code in updater.py is removed -- file contains only active logic"
    - "The unused config_loader.py module no longer exists in the codebase"
    - "pip install -r requirements.dev.txt succeeds without errors"
  artifacts:
    - path: "update_whitelist/updater.py"
      provides: "Clean updater with no commented-out dead code"
      excludes: "#.*print\|#.*old\|#.*legacy\|#.*deprecated"
    - path: "requirements.dev.txt"
      provides: "Valid pip requirements file with no syntax errors"
      contains: "pytest-cov~=5.0.0"
      excludes: "pytest-cov~=5.0.0\""
  key_links:
    - from: "update_whitelist/config/config_loader.py"
      to: "DELETE"
      via: "file removal"
      pattern: "file does not exist"
---

<objective>
Remove dead code and fix the build toolchain.

Purpose: Dead code confuses maintainers. A broken requirements file blocks fresh installs. Both are easy wins for code quality.

Output: config_loader.py deleted, updater.py cleaned of commented-out code, requirements.dev.txt syntax fixed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- config_loader.py is NOT imported anywhere (verified via grep) -->
<!-- It contains a single function: load_config(config_path) that does yaml.safe_load -->
<!-- The new load_config() in config.py has completely replaced it -->

<!-- updater.py current state: clean after Phase 1/2, need to verify no commented-out dead code remains -->
From update_whitelist/updater.py:
```python
# Current updater.py is 122 lines, all active logic
# Contains comments like "Per D-13:" which are active documentation, not dead code
# Contains "# ADD new rules FIRST (D-13)" which is active documentation, not dead code
```

<!-- requirements.dev.txt current state -->
```text
# Dev dependencies
pytest~=8.2.2
pytest-mock~=3.14.0
pytest-cov~=5.0.0"    <-- trailing quote on this line
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete unused config_loader.py and verify updater.py has no dead code</name>
  <files>update_whitelist/config/config_loader.py, update_whitelist/updater.py</files>
  <read_first>
    update_whitelist/config/config_loader.py
    update_whitelist/updater.py
  </read_first>
  <action>
    Per D-04 and D-05:

    **Delete config_loader.py** (per D-04):
    - Delete the file `update_whitelist/config/config_loader.py`
    - This module contains a single `load_config()` function that is superseded by the Pydantic-based `load_config()` in `config.py`
    - No other file imports from config_loader.py (verified: grep for "config_loader" returns 0 matches across all .py files)
    - There is no `__init__.py` in the config directory, so no import cleanup needed there either

    **Verify updater.py has no commented-out dead code** (per D-05):
    - Read updater.py and check for any lines that are commented-out Python code (lines starting with `#` followed by what looks like code, e.g., `# old_function(...)`, `# result = ...`, `# print(...)`)
    - Active documentation comments (like `# Per D-13:`, `# ADD new rules FIRST`, `# 遍历每个云服务提供商`) should be KEPT -- these explain intent
    - Only remove lines that are clearly disabled code, not explanatory comments
    - If no commented-out dead code is found (expected per CONTEXT.md), no changes to updater.py are needed
  </action>
  <verify>
    <automated>cd /Users/abrahamgreyson/Code/stay-in-whitelist && test ! -f update_whitelist/config/config_loader.py && echo "PASS: config_loader.py deleted" || echo "FAIL: config_loader.py still exists"</automated>
  </verify>
  <done>
    - File update_whitelist/config/config_loader.py does not exist
    - grep for "config_loader" across all .py files returns 0 matches
    - updater.py contains no commented-out code lines (only documentation comments remain)
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix trailing quote in requirements.dev.txt</name>
  <files>requirements.dev.txt</files>
  <read_first>
    requirements.dev.txt
  </read_first>
  <action>
    Per D-06:

    Fix line 4 of requirements.dev.txt:
    - Current: `pytest-cov~=5.0.0"`
    - Change to: `pytest-cov~=5.0.0`
    - Remove the trailing double-quote character at the end of the line
    - Do NOT change any other lines

    The file should read after the fix:
    ```
    # Dev dependencies
    pytest~=8.2.2
    pytest-mock~=3.14.0
    pytest-cov~=5.0.0
    ```
  </action>
  <verify>
    <automated>cd /Users/abrahamgreyson/Code/stay-in-whitelist && grep -c 'pytest-cov~=5.0.0"' requirements.dev.txt</automated>
  </verify>
  <done>
    - `requirements.dev.txt` line 4 is exactly `pytest-cov~=5.0.0` with no trailing quote
    - `grep '5.0.0"' requirements.dev.txt` returns 0 matches
    - File has exactly 4 lines (1 comment + 3 dependencies)
  </done>
</task>

</tasks>

<verification>
1. `test ! -f update_whitelist/config/config_loader.py` succeeds (file deleted)
2. `grep -rn "config_loader" --include="*.py" .` returns 0 matches (no stale imports)
3. `grep '5.0.0"' requirements.dev.txt` returns exit code 1 (no trailing quote)
4. `python -m pytest tests/ -x -q` passes (no regressions)
</verification>

<success_criteria>
- config_loader.py deleted and no references to it remain
- updater.py contains only active code and documentation comments
- requirements.dev.txt has valid syntax with no trailing quote on line 4
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-code-quality/03-02-SUMMARY.md`
</output>
