---
phase: 03-code-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - main.py
  - update_whitelist/cloud_providers/huawei_cloud.py
  - update_whitelist/cloud_providers/tencent_cloud.py
  - update_whitelist/ip_fetcher.py
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02

must_haves:
  truths:
    - "All runtime output appears in the log file via logger calls -- no print() statements remain in production code"
    - "API tokens and credentials never appear in log output"
  artifacts:
    - path: "main.py"
      provides: "Config load errors logged via logger.error instead of print"
      contains: "logger.error"
    - path: "update_whitelist/cloud_providers/huawei_cloud.py"
      provides: "Delete response logged via logger.debug instead of print"
      contains: "logger.debug"
    - path: "update_whitelist/cloud_providers/tencent_cloud.py"
      provides: "Add/delete responses logged via logger.debug instead of print; logger import added"
      contains: "logger.debug"
  key_links:
    - from: "main.py"
      to: "update_whitelist/logger.py"
      via: "logger = get_logger() at module level"
      pattern: "get_logger"
    - from: "update_whitelist/cloud_providers/tencent_cloud.py"
      to: "update_whitelist/logger.py"
      via: "new import of get_logger"
      pattern: "from.*logger.*import.*get_logger"
---

<objective>
Replace all print() calls with logger calls and audit that no credentials leak into logs.

Purpose: Ensure all output goes through structured logging with rotation. print() bypasses log rotation, losing operational history in a systemd deployment.

Output: Zero print() calls in production .py files. Verified that no access_key, secret_key, or URL tokens appear in log messages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Logger factory used across all modules -->
From update_whitelist/logger.py:
```python
def get_logger(name=__name__):
    """Returns a logger with StreamHandler + TimedRotatingFileHandler."""

def reconfigure_logging(log_file_path: str) -> None:
    """Replace TimedRotatingFileHandler with new path."""
```

<!-- main.py already has logger imported at line 18 -->
```python
logger = get_logger()
```

<!-- huawei_cloud.py already has logger imported at line 15-17 -->
```python
from update_whitelist.logger import get_logger
logger = get_logger()
```

<!-- tencent_cloud.py has NO logger import currently -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace print() with logger calls in main.py and cloud providers</name>
  <files>main.py, update_whitelist/cloud_providers/huawei_cloud.py, update_whitelist/cloud_providers/tencent_cloud.py</files>
  <read_first>
    main.py
    update_whitelist/cloud_providers/huawei_cloud.py
    update_whitelist/cloud_providers/tencent_cloud.py
    update_whitelist/logger.py
  </read_first>
  <action>
    Per D-01 and D-02, replace all 5 print() calls with logger calls.

    **main.py** (lines 69, 72) -- per D-01:
    - Line 69: Change `print(f"配置加载失败: {e}")` to `logger.error(f"配置加载失败: {e}")`
    - Line 72: Change `print(f"配置加载出错: {e}")` to `logger.error(f"配置加载出错: {e}")`
    - NOTE: main.py already has `logger = get_logger()` at line 18. No import changes needed.

    **update_whitelist/cloud_providers/huawei_cloud.py** (line 32) -- per D-02:
    - Line 32: Change `print(response)` to `logger.debug(response)`
    - NOTE: huawei_cloud.py already has `from update_whitelist.logger import get_logger` at line 15 and `logger = get_logger()` at line 17. No import changes needed.

    **update_whitelist/cloud_providers/tencent_cloud.py** (lines 56, 78) -- per D-02:
    - Add import at the top of the file (after existing imports, before the class): `from update_whitelist.logger import get_logger`
    - Add logger initialization after the import: `logger = get_logger()`
    - Line 56: Change `print(resp.to_json_string())` to `logger.debug(resp.to_json_string())`
    - Line 78: Change `print(resp.to_json_string())` to `logger.debug(resp.to_json_string())`
  </action>
  <verify>
    <automated>cd /Users/abrahamgreyson/Code/stay-in-whitelist && grep -rn "print(" --include="*.py" main.py update_whitelist/cloud_providers/ update_whitelist/updater.py update_whitelist/ip_fetcher.py update_whitelist/config/ update_whitelist/logger.py | grep -v "__pycache__" | grep -v "# print" | head -20</automated>
  </verify>
  <done>
    - grep for `print(` in main.py returns 0 matches
    - grep for `print(` in huawei_cloud.py returns 0 matches
    - grep for `print(` in tencent_cloud.py returns 0 matches
    - tencent_cloud.py contains `from update_whitelist.logger import get_logger`
    - tencent_cloud.py contains `logger = get_logger()`
    - main.py contains `logger.error(f"配置加载失败`
    - main.py contains `logger.error(f"配置加载出错`
    - huawei_cloud.py contains `logger.debug(response)`
    - tencent_cloud.py contains `logger.debug(resp.to_json_string())`
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit log messages for credential leakage</name>
  <files>update_whitelist/ip_fetcher.py, update_whitelist/cloud_providers/huawei_cloud.py, update_whitelist/cloud_providers/tencent_cloud.py</files>
  <read_first>
    update_whitelist/ip_fetcher.py
    update_whitelist/cloud_providers/huawei_cloud.py
    update_whitelist/cloud_providers/tencent_cloud.py
  </read_first>
  <action>
    Per D-03, audit all logger calls across these 3 files to confirm no credential leakage exists.

    Verify these specific conditions:
    1. `ip_fetcher.py` -- The URL with token is NEVER logged. Confirm that the logger calls at lines 51, 55, 61, 64, 66, 68, 70 use provider name only, not the full URL with `?token=...`. The `url` variable containing the token is used only in `requests.get()`, never in any logger call.
    2. `huawei_cloud.py` -- After the print->logger change, `logger.debug(response)` logs the SDK response object. Confirm that the response object does not contain access_key or secret_key. The Huawei VPC SDK response contains security group rule data (IDs, IPs, ports), not credentials.
    3. `tencent_cloud.py` -- After the print->logger change, `logger.debug(resp.to_json_string())` logs the JSON response. Confirm that the JSON response contains only security group policy data (PolicyIndex, Port, CidrBlock, Protocol), not access_key or secret_key.

    If any credential leakage is found (unlikely per D-03), add a sanitization step. The current assessment is that no changes are needed -- this task is a verification audit.

    If the audit passes (expected), no code changes are required for this task. Document the audit result in the commit message.
  </action>
  <verify>
    <automated>cd /Users/abrahamgreyson/Code/stay-in-whitelist && grep -rn "access_key\|secret_key\|token" update_whitelist/ip_fetcher.py update_whitelist/cloud_providers/huawei_cloud.py update_whitelist/cloud_providers/tencent_cloud.py | grep -i "log" | grep -v "__pycache__" | head -20</automated>
  </verify>
  <done>
    - No logger call in ip_fetcher.py contains a URL with a token parameter
    - No logger call in huawei_cloud.py contains access_key or secret_key values
    - No logger call in tencent_cloud.py contains access_key or secret_key values
    - grep for access_key/secret_key near logger calls returns 0 matches
  </done>
</task>

</tasks>

<verification>
1. `grep -rn "print(" --include="*.py" main.py update_whitelist/cloud_providers/ update_whitelist/ip_fetcher.py update_whitelist/updater.py update_whitelist/config/ update_whitelist/logger.py | grep -v __pycache__` returns 0 results (no print statements in production code)
2. `python -m pytest tests/ -x -q` passes (no regressions from logger changes)
</verification>

<success_criteria>
- Zero print() calls remain in any production .py file (main.py, cloud providers, updater, ip_fetcher, config, logger)
- tencent_cloud.py has logger import and get_logger() call
- All log messages audited and confirmed free of credential leakage
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-code-quality/03-01-SUMMARY.md`
</output>
