---
phase: 05-operational-deployment
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified: [README.md]
autonomous: true
requirements: [OPS-02]
user_setup: []
must_haves:
  truths:
    - "README contains clear systemd deployment instructions"
    - "Users can deploy the service by following README steps"
    - "Service customization options are documented"
  artifacts:
    - path: "README.md"
      provides: "Deployment documentation for systemd service"
      contains: "systemd" or "systemctl"
  key_links:
    - from: "README.md"
      to: "stay-in-whitelist.service"
      via: "deployment instructions"
      pattern: "stay-in-whitelist\\.service"
    - from: "README.md"
      to: "config.yaml"
      via: "configuration documentation"
      pattern: "config\\.yaml"
---

<objective>
Update README.md with comprehensive systemd deployment instructions, including service installation, path customization, and operational management.

Purpose: Enable users to deploy Stay in Whitelist as a production systemd service by following clear, step-by-step documentation.
Output: Complete deployment guide in README.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-operational-deployment/05-CONTEXT.md
@.planning/phases/05-operational-deployment/05-RESEARCH.md

<interfaces>
<!-- Current README structure -->

From README.md (read in planning phase):
- Project description and badges
- Features section
- Quick start
- Configuration
- Architecture
- License section

Need to add:
- Deployment section (after Configuration, before Architecture)
- Systemd service setup instructions
- Service management commands
- Troubleshooting tips

From CONTEXT.md D-11:
"README 中添加部署说明：复制服务文件到 /etc/systemd/system/，自定义路径，启用服务"

From RESEARCH.md Code Examples section:
```bash
# Copy template and replace placeholders
sudo cp stay-in-whitelist.service /etc/systemd/system/
sudo sed -i 's|%USER%|yourusername|g' /etc/systemd/system/stay-in-whitelist.service
sudo sed -i 's|%GROUP%|yourgroup|g' /etc/systemd/system/stay-in-whitelist.service
sudo sed -i 's|/absolute/path/to|/home/yourusername|g' /etc/systemd/system/stay-in-whitelist.service

# Enable and start
sudo systemctl daemon-reload
sudo systemctl enable stay-in-whitelist
sudo systemctl start stay-in-whitelist

# Check status
sudo systemctl status stay-in-whitelist
journalctl -u stay-in-whitelist -f
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add systemd deployment section to README</name>
  <files>README.md</files>
  <action>
    Add a comprehensive "部署 (Deployment)" section to README.md after the "配置 (Configuration)" section and before "架构 (Architecture)".

    The section should include:

    ## 部署 (Deployment)

    ### systemd 服务配置

    Stay in Whitelist 可以作为 systemd 服务长期运行，实现开机自启动和故障自动恢复。

    #### 1. 安装服务

    ```bash
    # 复制服务模板到 systemd 目录
    sudo cp stay-in-whitelist.service /etc/systemd/system/

    # 编辑服务文件，自定义路径（重要！）
    sudo nano /etc/systemd/system/stay-in-whitelist.service
    ```

    #### 2. 自定义路径

    在服务文件中修改以下路径为实际部署位置：

    ```ini
    [Service]
    # 修改为项目实际路径
    WorkingDirectory=/home/yourusername/stay-in-whitelist
    ExecStart=/home/yourusername/stay-in-whitelist/venv/bin/python /home/yourusername/stay-in-whitelist/main.py

    # 修改日志路径（可选，默认使用项目内的 stay_in_whitelist.log）
    StandardOutput=append:/home/yourusername/stay-in-whitelist/stay_in_whitelist.log
    StandardError=append:/home/yourusername/stay-in-whitelist/stay_in_whitelist.log
    ```

    **路径说明：**
    - `WorkingDirectory`: 项目根目录，包含 main.py 和 config.yaml
    - `ExecStart`: Python 解释器路径（venv/bin/python）和 main.py 完整路径
    - `StandardOutput`/`StandardError`: 日志文件路径，需确保有写入权限

    #### 3. 启动服务

    ```bash
    # 重新加载 systemd 配置
    sudo systemctl daemon-reload

    # 启用开机自启动
    sudo systemctl enable stay-in-whitelist

    # 启动服务
    sudo systemctl start stay-in-whitelist

    # 查看服务状态
    sudo systemctl status stay-in-whitelist
    ```

    #### 4. 服务管理

    ```bash
    # 查看实时日志
    sudo journalctl -u stay-in-whitelist -f

    # 查看最近 100 行日志
    sudo journalctl -u stay-in-whitelist -n 100

    # 停止服务
    sudo systemctl stop stay-in-whitelist

    # 重启服务
    sudo systemctl restart stay-in-whitelist

    # 禁用开机自启动
    sudo systemctl disable stay-in-whitelist
    ```

    ### 日志管理

    服务运行后，日志文件位于项目根目录的 `stay_in_whitelist.log`。

    **日志轮转配置：**
    - 每日午夜自动轮转
    - 保留最近 30 天的日志文件
    - 旧日志自动删除，避免磁盘占用过多

    **查看日志文件：**
    ```bash
    # 查看当前日志
    tail -f stay_in_whitelist.log

    # 查看历史日志（轮转后的备份）
    ls -lh stay_in_whitelist.log*
    ```

    ### 故障排查

    **服务无法启动：**
    1. 检查路径配置是否正确（WorkingDirectory、ExecStart）
    2. 检查 Python 虚拟环境是否存在：`ls venv/bin/python`
    3. 检查 config.yaml 是否存在且格式正确
    4. 查看详细错误日志：`sudo journalctl -u stay-in-whitelist -n 50`

    **IP 检测失败：**
    1. 检查网络连接：`ping ipinfo.io`
    2. 检查 config.yaml 中的 IP 检测服务配置
    3. 查看日志中的错误信息

    **云服务白名单未更新：**
    1. 检查 config.yaml 中的云服务凭证是否正确
    2. 检查安全组 ID 和区域配置
    3. 确认 rule_prefix 配置正确（默认 "from Wulihe"）
    4. 查看日志中的 API 调用结果

    **日志文件过大：**
    - 日志会自动轮转，保留最近 30 天
    - 如果需要手动清理：`rm stay_in_whitelist.log.[N]`（N > 30）
    - 检查日志轮转是否正常：`ls -lh stay_in_whitelist.log*`

    Insert this section AFTER the Configuration section and BEFORE the Architecture section.

    Maintain Chinese language consistency with existing README content.
  </action>
  <verify>
    <automated>grep -q "## 部署 (Deployment)" /Users/abrahamgreyson/Code/stay-in-whitelist/README.md && grep -q "systemd 服务配置" /Users/abrahamgreyson/Code/stay-in-whitelist/README.md && grep -q "sudo systemctl" /Users/abrahamgreyson/Code/stay-in-whitelist/README.md && echo "README deployment section added"</automated>
  </verify>
  <done>README contains comprehensive systemd deployment instructions</done>
</task>

<task type="auto">
  <name>Task 2: Verify README structure and content quality</name>
  <files>README.md</files>
  <action>
    Verify README.md has correct section ordering and comprehensive deployment content.

    Check:
    1. Deployment section appears after Configuration and before Architecture
    2. All subsections present:
       - systemd 服务配置
       - 安装服务
       - 自定义路径
       - 启动服务
       - 服务管理
       - 日志管理
       - 故障排查
    3. Code blocks use correct bash syntax highlighting
    4. Chinese language consistent with existing content
    5. No markdown syntax errors

    Manual verification:
    ```bash
    # Check section ordering
    grep -n "^## " README.md
    ```

    Expected output should show:
    - ...
    - N: ## 配置 (Configuration)
    - N+1: ## 部署 (Deployment)
    - N+2: ## 架构 (Architecture)
    - ...
  </action>
  <verify>
    <automated>grep -n "^## " /Users/abrahamgreyson/Code/stay-in-whitelist/README.md | grep -A1 "配置" | grep -B1 "架构"</automated>
  </verify>
  <done>README deployment section verified for structure and quality</done>
</task>

<task type="auto">
  <name>Task 3: Update README table of contents if present</name>
  <files>README.md</files>
  <action>
    Check if README.md has a table of contents at the top. If present, add "部署 (Deployment)" entry between "配置 (Configuration)" and "架构 (Architecture)".

    Table of contents typically looks like:
    - [配置 (Configuration)](#configuration)
    - [部署 (Deployment)](#deployment)  <-- Add this
    - [架构 (Architecture)](#architecture)

    If no table of contents exists, skip this task.
  </action>
  <verify>
    <automated>grep -q "部署 (Deployment)" /Users/abrahamgreyson/Code/stay-in-whitelist/README.md || echo "No TOC found or already updated"</automated>
  </verify>
  <done>README table of contents updated if present</done>
</task>

</tasks>

<verification>
**Content Verification:**
- [ ] README contains "## 部署 (Deployment)" section
- [ ] Deployment section appears after Configuration, before Architecture
- [ ] systemd 服务配置 subsection present
- [ ] 安装服务 steps with code blocks
- [ ] 自定义路径 instructions with example paths
- [ ] 启动服务 commands (systemctl enable/start/status)
- [ ] 服务管理 commands (journalctl, stop, restart)
- [ ] 日志管理 section mentioning 30-day retention
- [ ] 故障排查 section with common issues

**Quality Verification:**
- [ ] All code blocks use ```bash syntax highlighting
- [ ] Chinese language consistent with existing content
- [ ] No markdown syntax errors
- [ ] Links and references valid

**User Experience Verification:**
- [ ] Steps are clear and sequential
- [ ] Path customization instructions prominent
- [ ] Common troubleshooting scenarios covered
- [ ] Log management explained
</verification>

<success_criteria>
1. ✅ README contains comprehensive systemd deployment section (OPS-02)
2. ✅ Deployment instructions clear and actionable
3. ✅ Path customization well-documented
4. ✅ Service management commands included
5. ✅ Troubleshooting guidance provided
6. ✅ Chinese language consistency maintained
</success_criteria>

<output>
After completion, create `.planning/phases/05-operational-deployment/05-03-SUMMARY.md`
</output>
