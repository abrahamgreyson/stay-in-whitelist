---
phase: 04-project-identity
plan: 02
type: execute
wave: 1
depends_on:
  - 04-01
files_modified:
  - pyproject.toml
  - .gitignore
  - .github/workflows/test.yml
  - .gitlab-ci.yml
  - config.example.yaml
autonomous: true
requirements:
  - IDENT-01
  - IDENT-02
  - IDENT-03

must_haves:
  truths:
    - "pyproject.toml declares package name as stay-in-whitelist with correct write_to path"
    - "CI pipelines reference stay_in_whitelist for coverage and artifact paths"
    - ".gitignore references stay_in_whitelist for log file and _version.py"
    - "config.example.yaml comments reference the new log filename stay_in_whitelist.log"
  artifacts:
    - path: "pyproject.toml"
      provides: "Package name and setuptools_scm write_to updated"
      contains: "stay-in-whitelist"
    - path: ".gitignore"
      provides: "Ignores new package log and version file"
      contains: "stay_in_whitelist"
    - path: ".github/workflows/test.yml"
      provides: "CI coverage target updated"
      contains: "stay_in_whitelist"
    - path: ".gitlab-ci.yml"
      provides: "CI coverage and artifact paths updated"
      contains: "stay_in_whitelist"
    - path: "config.example.yaml"
      provides: "Comments reference new log filename"
      contains: "stay_in_whitelist.log"
  key_links:
    - from: "pyproject.toml"
      to: "setuptools_scm"
      via: "write_to field"
      pattern: "write_to.*stay_in_whitelist"
    - from: ".github/workflows/test.yml"
      to: "pytest --cov"
      via: "coverage target"
      pattern: "--cov=stay_in_whitelist"
---

<objective>
Update config, build, and CI files to reference the new project name "stay_in_whitelist" before the directory rename happens.

Purpose: By updating all string references first (package name in pyproject.toml, CI --cov flags, gitignore entries, config comments), the atomic rename in Plan 03 becomes a cleaner operation limited to directory rename + Python import updates. This avoids mixing string-only edits with structural changes in the same task.

Output: pyproject.toml, .gitignore, CI files, and config.example.yaml all reference stay_in_whitelist/stay-in-whitelist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-project-identity/04-CONTEXT.md

<interfaces>
<!-- Files being modified and their current relevant content -->

From pyproject.toml:
  line 2: name = "update-whitelist"
  line 44: write_to = "update_whitelist/_version.py"

From .gitignore:
  line 8: update_whitelist.log*
  line 14: update_whitelist/_version.py

From .github/workflows/test.yml:
  line 43: pytest tests/ --cov=update_whitelist --junitxml=report.xml --cov-report=xml --cov-report=term

From .gitlab-ci.yml:
  line 29: - update_whitelist/_version.py (in artifacts paths)
  line 40: pytest tests/ --cov=update_whitelist --junitxml=report.xml --cov-report=xml --cov-report=term

From config.example.yaml:
  line 60: #   log_file: /var/log/stay-in-whitelist/update_whitelist.log

From logger.py (NOT modified in this plan -- for reference only):
  line 26: TimedRotatingFileHandler('update_whitelist.log', ...)
  Note: logger.py default filename changes in Plan 03 after the rename.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update pyproject.toml package name and setuptools_scm write_to</name>
  <files>pyproject.toml</files>
  <action>
Per D-12, D-13 from CONTEXT.md:

1. Change `name = "update-whitelist"` (line 2) to `name = "stay-in-whitelist"`
2. Change `write_to = "update_whitelist/_version.py"` (line 44) to `write_to = "stay_in_whitelist/_version.py"`

Do NOT change anything else. These are the only two occurrences of the old package name in pyproject.toml.
  </action>
  <verify>
    <automated>cd /Users/abrahamgreyson/Code/stay-in-whitelist && grep -c "stay-in-whitelist" pyproject.toml && grep -c "stay_in_whitelist/_version.py" pyproject.toml</automated>
  </verify>
  <done>pyproject.toml has name="stay-in-whitelist" and write_to="stay_in_whitelist/_version.py". No occurrence of "update-whitelist" or "update_whitelist/_version.py" remains.</done>
</task>

<task type="auto">
  <name>Task 2: Update .gitignore, CI configs, and config.example.yaml string references</name>
  <files>.gitignore, .github/workflows/test.yml, .gitlab-ci.yml, config.example.yaml</files>
  <action>
Per D-08, D-11, D-14 from CONTEXT.md:

1. **.gitignore**:
   - Change `update_whitelist.log*` (line 8) to `stay_in_whitelist.log*`
   - Change `update_whitelist/_version.py` (line 14) to `stay_in_whitelist/_version.py`

2. **.github/workflows/test.yml**:
   - Change `--cov=update_whitelist` (line 43) to `--cov=stay_in_whitelist`

3. **.gitlab-ci.yml**:
   - Change `- update_whitelist/_version.py` (line 29) to `- stay_in_whitelist/_version.py`
   - Change `--cov=update_whitelist` (line 40) to `--cov=stay_in_whitelist`

4. **config.example.yaml**:
   - Change the comment `update_whitelist.log` (line 60) to `stay_in_whitelist.log` so the example shows the new filename. Per D-11, only update comment references, not config keys.

After all edits, verify no `update_whitelist` or `update-whitelist` string remains in any of these four files (excluding any matches inside .planning/ directory).
  </action>
  <verify>
    <automated>cd /Users/abrahamgreyson/Code/stay-in-whitelist && grep -c "update_whitelist\|update-whitelist" .gitignore .github/workflows/test.yml .gitlab-ci.yml config.example.yaml || echo "No old references found (expected grep exit 1)"</automated>
  </verify>
  <done>All four files reference stay_in_whitelist/stay-in-whitelist. No old package name references remain. Config keys (huawei, tencent, ipinfo, timeouts, paths, check_interval) are unchanged per D-10.</done>
</task>

</tasks>

<verification>
1. `grep -rn "update.whitelist\|update-whitelist" pyproject.toml .gitignore .github/workflows/test.yml .gitlab-ci.yml config.example.yaml` -- returns no matches
2. `grep "stay-in-whitelist" pyproject.toml` -- finds package name
3. `grep "stay_in_whitelist" .gitignore` -- finds log and _version entries
4. `grep "stay_in_whitelist" .github/workflows/test.yml .gitlab-ci.yml` -- finds --cov references
5. `grep "stay_in_whitelist.log" config.example.yaml` -- finds comment reference
</verification>

<success_criteria>
- pyproject.toml: name="stay-in-whitelist", write_to="stay_in_whitelist/_version.py" (per D-13)
- .gitignore: stay_in_whitelist.log*, stay_in_whitelist/_version.py entries
- CI files: --cov=stay_in_whitelist in both GitHub Actions and GitLab CI (per D-14)
- config.example.yaml: log filename comment updated to stay_in_whitelist.log (per D-08, D-11)
- Zero remaining "update_whitelist" or "update-whitelist" references in these 5 files
</success_criteria>

<output>
After completion, create `.planning/phases/04-project-identity/04-02-SUMMARY.md`
</output>
