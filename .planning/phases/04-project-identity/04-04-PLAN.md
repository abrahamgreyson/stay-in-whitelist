---
phase: 04-project-identity
plan: 04
type: execute
wave: 3
depends_on:
  - 04-03
files_modified:
  - README.md
autonomous: false
requirements:
  - IDENT-04

must_haves:
  truths:
    - "README.md describes the project as Stay in Whitelist"
    - "Badge URLs point to the correct GitHub repository"
    - "Architecture section references stay_in_whitelist package"
    - "Configuration docs show current field names including rule_prefix"
    - "Migration notes warn about the package rename and log file change"
  artifacts:
    - path: "README.md"
      provides: "Full README with updated branding, badges, architecture, config, migration"
      min_lines: 80
  key_links:
    - from: "README.md"
      to: "stay_in_whitelist/"
      via: "code path references"
      pattern: "stay_in_whitelist"
---

<objective>
Rewrite README.md with full Stay in Whitelist branding, updated architecture, current configuration fields, and migration guidance for existing users.

Purpose: The README is the first thing users see. After the package rename and feature additions (rule_prefix, timeout, paths), the README must accurately reflect the current project state. This is a full rewrite, not a find-replace.

Output: README.md with correct branding, badges, architecture, configuration docs, and migration notes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-project-identity/04-CONTEXT.md
@.planning/phases/04-project-identity/04-03-SUMMARY.md
@README.md

<interfaces>
<!-- Current README and project state for reference -->

From README.md:
  Current: 173 lines, Chinese, references update-whitelist and old architecture

Project state after Plans 01-03:
  Package: stay_in_whitelist (renamed from update_whitelist)
  Entry: main.py imports from stay_in_whitelist
  Config fields: huawei, tencent, aliyun, ipinfo, timeouts, paths, check_interval, rule_prefix
  Default log: stay_in_whitelist.log
  Providers: Huawei Cloud (VPC SDK), Tencent Cloud (VPC SDK)
  Rule prefix: configurable, default "from Wulihe"
  Python: 3.9+
  CI: GitHub Actions + GitLab CI
  Run: python main.py or systemd service

Badge URL correction (D-07):
  Current: abrahamgreyson/update-whitelist (incorrect)
  Correct: verify from git remote or use the actual repo path
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md with Stay in Whitelist branding and current architecture</name>
  <files>README.md</files>
  <action>
Per D-06, D-07 from CONTEXT.md:

Rewrite README.md entirely in Chinese with the following structure:

1. **Title and badges**: Project name "Stay in Whitelist", badges with corrected GitHub repo URL (per D-07). Check `git remote get-url origin` for the actual repo path.

2. **Project description**: What it does -- periodic IP detection, automatic security group whitelist updates, supports Huawei Cloud and Tencent Cloud.

3. **Features**: Key features list (multi-cloud, configurable rule prefix, dev/prod isolation, systemd support).

4. **Quick start**: Clone, configure config.yaml from config.example.yaml, run.

5. **Configuration**: Document all config fields including:
   - rule_prefix (new, with default "from Wulihe" and dev/prod isolation use case)
   - timeouts (request timeouts)
   - paths (log_file, ip_cache)
   - check_interval
   - Cloud provider sections (huawei, tencent)

6. **Architecture**: Describe the polling pattern, strategy pattern for cloud providers, config-driven approach. Reference stay_in_whitelist package (not update_whitelist).

7. **Deployment**: systemd service configuration, log management.

8. **Migration guide**: Note the package rename from update_whitelist to stay_in_whitelist, log file name change, and that existing config.yaml keys are unchanged.

9. **Development**: How to run tests, Python version requirements.

Write in clean, professional Chinese. Keep it concise -- this is a personal tool, not enterprise software.
  </action>
  <verify>
    <automated>cd /Users/abrahamgreyson/Code/stay-in-whitelist && test $(wc -l < README.md) -ge 80 && grep -c "Stay in Whitelist\|stay_in_whitelist\|stay-in-whitelist" README.md | grep -v "^0$"</automated>
  </verify>
  <done>README.md fully rewritten in Chinese with Stay in Whitelist branding, correct badge URLs, current architecture, all config fields documented, and migration notes for existing users.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify README content accuracy</name>
  <action>
Human reviews the rewritten README.md for accuracy and completeness.

What was built: README.md full rewrite with Stay in Whitelist branding, architecture, config docs, and migration guide.

Steps to verify:
  1. Open README.md in a text editor or viewer
  2. Verify the title says "Stay in Whitelist" (not update-whitelist)
  3. Check that badge URLs are correct (not pointing to wrong repo)
  4. Verify architecture section references stay_in_whitelist package
  5. Verify config section includes rule_prefix field documentation
  6. Verify migration guide mentions the package rename
  7. Overall: Does the README accurately describe the project as it exists after Plans 01-03?
  </action>
  <verify>User types "approved" or describes issues to fix</verify>
  <done>README content verified by user as accurate and complete</done>
</task>

</tasks>

<verification>
1. `wc -l README.md` -- at least 80 lines (comprehensive but not bloated)
2. `grep -c "update.whitelist\|update-whitelist" README.md` -- only in migration section context, not as current name
3. `grep -c "Stay in Whitelist" README.md` -- appears in title/description
4. `grep -c "stay_in_whitelist" README.md` -- appears in code path references
5. `grep -c "rule_prefix" README.md` -- config field documented
</verification>

<success_criteria>
- README.md describes the project as "Stay in Whitelist" (per D-06)
- Badge URLs correct (per D-07)
- Architecture section references stay_in_whitelist package
- Configuration section includes rule_prefix with dev/prod isolation guidance
- Migration notes cover package rename and log file change
- Written entirely in Chinese
- No "update_whitelist" or "update-whitelist" used as the current project name
</success_criteria>

<output>
After completion, create `.planning/phases/04-project-identity/04-04-SUMMARY.md`
</output>
