---
phase: 04-project-identity
plan: 03
type: execute
wave: 2
depends_on:
  - 04-02
files_modified:
  - main.py
  - update_whitelist/__init__.py
  - update_whitelist/_version.py
  - update_whitelist/logger.py
  - update_whitelist/ip_fetcher.py
  - update_whitelist/updater.py
  - update_whitelist/config/config.py
  - update_whitelist/cloud_providers/base_cloud_provider.py
  - update_whitelist/cloud_providers/huawei_cloud.py
  - update_whitelist/cloud_providers/tencent_cloud.py
  - tests/conftest.py
  - tests/test_ip_fetcher.py
  - tests/test_updater.py
  - tests/test_huawei_cloud.py
  - tests/test_tencent_cloud.py
  - tests/test_config.py
  - tests/test_base_cloud_provider.py
autonomous: true
requirements:
  - IDENT-01
  - IDENT-02
  - IDENT-03

must_haves:
  truths:
    - "The package is importable as stay_in_whitelist (not update_whitelist)"
    - "All Python imports use from stay_in_whitelist... (not from update_whitelist...)"
    - "All test mocker.patch paths use stay_in_whitelist (not update_whitelist)"
    - "Log messages, comments, and string literals reference new branding -- no old name remains"
    - "The default log filename is stay_in_whitelist.log"
    - "The package directory on disk is stay_in_whitelist/ (not update_whitelist/)"
  artifacts:
    - path: "stay_in_whitelist/"
      provides: "Renamed package directory"
      contains: "__init__.py"
    - path: "main.py"
      provides: "Updated imports from stay_in_whitelist"
      contains: "from stay_in_whitelist"
    - path: "stay_in_whitelist/logger.py"
      provides: "Default log filename updated"
      contains: "stay_in_whitelist.log"
  key_links:
    - from: "main.py"
      to: "stay_in_whitelist/"
      via: "import statements"
      pattern: "from stay_in_whitelist"
    - from: "stay_in_whitelist/config/config.py"
      to: "stay_in_whitelist/"
      via: "relative imports and module docstring"
      pattern: "stay_in_whitelist"
    - from: "tests/"
      to: "stay_in_whitelist/"
      via: "import paths and mocker.patch targets"
      pattern: "stay_in_whitelist"
---

<objective>
Rename the Python package directory from update_whitelist/ to stay_in_whitelist/ and update all imports, test paths, log messages, comments, and string literals to match the new identity.

Purpose: This is the core structural change. After Plan 02 updated all config/build/CI string references, this plan handles the atomic directory rename and all Python-level changes: imports, mocker.patch paths, log file default, and any remaining old branding in comments or strings.

Output: Package importable as stay_in_whitelist, all Python files updated, full test suite passes with new import paths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-project-identity/04-CONTEXT.md
@.planning/phases/04-project-identity/04-02-SUMMARY.md

<interfaces>
<!-- Files affected by rename and their import patterns -->

Source files with update_whitelist imports:
  main.py lines 12-15:
    from update_whitelist.config.config import load_config
    from update_whitelist.ip_fetcher import get_current_ip, load_cached_ip, cache_ip
    from update_whitelist.updater import Updater
    from update_whitelist.logger import get_logger, reconfigure_logging

  update_whitelist/cloud_providers/tencent_cloud.py line 13:
    from update_whitelist.logger import get_logger

  update_whitelist/cloud_providers/huawei_cloud.py line 15:
    from update_whitelist.logger import get_logger

  update_whitelist/cloud_providers/base_cloud_provider.py line 12:
    from update_whitelist.logger import get_logger

  update_whitelist/config/config.py line 9:
    from update_whitelist.config.config import load_config

String/comment references to audit:
  update_whitelist/config/config.py line 91:
    "in the project root (parent of update_whitelist/)"
  update_whitelist/logger.py line 26:
    TimedRotatingFileHandler('update_whitelist.log', ...)

Test files with import and mocker.patch paths:
  tests/conftest.py - imports from update_whitelist.config.config
  tests/test_ip_fetcher.py - imports and mocker.patch('update_whitelist.ip_fetcher.requests')
  tests/test_updater.py - imports and mocker.patch paths
  tests/test_huawei_cloud.py - imports and mocker.patch paths
  tests/test_tencent_cloud.py - imports, mocker.patch, and "from Wulihe" assertions
  tests/test_config.py - imports and docstring references
  tests/test_base_cloud_provider.py - import paths
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename package directory and update all Python imports and paths</name>
  <files>main.py, stay_in_whitelist/__init__.py, stay_in_whitelist/logger.py, stay_in_whitelist/ip_fetcher.py, stay_in_whitelist/updater.py, stay_in_whitelist/config/config.py, stay_in_whitelist/cloud_providers/base_cloud_provider.py, stay_in_whitelist/cloud_providers/huawei_cloud.py, stay_in_whitelist/cloud_providers/tencent_cloud.py, tests/conftest.py, tests/test_ip_fetcher.py, tests/test_updater.py, tests/test_huawei_cloud.py, tests/test_tencent_cloud.py, tests/test_config.py, tests/test_base_cloud_provider.py</files>
  <action>
Per D-12, D-15, D-16 from CONTEXT.md:

This task performs the atomic package rename in a precise order to avoid broken intermediate states.

**Step 1 -- Directory rename:**
```bash
git mv update_whitelist/ stay_in_whitelist/
```

**Step 2 -- Update all Python imports in renamed package files:**
In every file under `stay_in_whitelist/`, replace `update_whitelist` with `stay_in_whitelist` in import statements. Specific files:

- `stay_in_whitelist/cloud_providers/tencent_cloud.py` line 13: `from update_whitelist.logger` -> `from stay_in_whitelist.logger`
- `stay_in_whitelist/cloud_providers/huawei_cloud.py` line 15: `from update_whitelist.logger` -> `from stay_in_whitelist.logger`
- `stay_in_whitelist/cloud_providers/base_cloud_provider.py` line 12: `from update_whitelist.logger` -> `from stay_in_whitelist.logger`
- `stay_in_whitelist/config/config.py` line 9: `from update_whitelist.config.config` -> `from stay_in_whitelist.config.config`

**Step 3 -- Update main.py imports:**
Lines 12-15: Change all `from update_whitelist.` to `from stay_in_whitelist.`

**Step 4 -- Update logger.py default filename:**
`stay_in_whitelist/logger.py` line 26: Change `'update_whitelist.log'` to `'stay_in_whitelist.log'`
Per D-08, D-09.

**Step 5 -- Update comment in config.py:**
`stay_in_whitelist/config/config.py` line 91: Change `parent of update_whitelist/` to `parent of stay_in_whitelist/`

**Step 6 -- Update all test files:**
In each test file, replace `update_whitelist` with `stay_in_whitelist` in:
- Import statements (e.g., `from update_whitelist.config.config import Config` -> `from stay_in_whitelist.config.config import Config`)
- mocker.patch target paths (e.g., `mocker.patch('update_whitelist.ip_fetcher.requests')` -> `mocker.patch('stay_in_whitelist.ip_fetcher.requests')`)
- Docstrings referencing the old package name

Files: tests/conftest.py, tests/test_ip_fetcher.py, tests/test_updater.py, tests/test_huawei_cloud.py, tests/test_tencent_cloud.py, tests/test_config.py, tests/test_base_cloud_provider.py

**Step 7 -- Verify no old references remain:**
After all edits, run:
```bash
grep -rn "update_whitelist" --include="*.py" stay_in_whitelist/ main.py tests/
```
This MUST return zero matches. If any remain, fix them.
  </action>
  <verify>
    <automated>cd /Users/abrahamgreyson/Code/stay-in-whitelist && python -m pytest tests/ -x -q && grep -rn "update_whitelist" --include="*.py" stay_in_whitelist/ main.py tests/; test $? -eq 0 || echo "Old references remain!"</automated>
  </verify>
  <done>Package directory is stay_in_whitelist/, all imports and mocker.patch paths updated, logger default filename is stay_in_whitelist.log, config.py comment updated, full test suite passes, zero remaining "update_whitelist" in Python source.</done>
</task>

<task type="auto">
  <name>Task 2: Audit and update remaining old branding in log messages, comments, and string literals</name>
  <files>stay_in_whitelist/*.py, stay_in_whitelist/**/*.py, main.py, tests/*.py</files>
  <action>
Per IDENT-02 requirement: "All user-facing strings, log messages, and comments updated to Stay in Whitelist branding"

This task catches anything the import rename missed -- string literals, log messages, and comments that reference the old project name.

1. Search for any remaining `update_whitelist` or `update-whitelist` in string literals and comments across all Python files:
   ```bash
   grep -rn "update.whitelist\|update-whitelist\|Update Whitelist\|updateWhitelist" --include="*.py" stay_in_whitelist/ main.py tests/
   ```

2. For each match found, update the reference:
   - In string literals: change to `stay_in_whitelist`, `stay-in-whitelist`, or `Stay in Whitelist` as contextually appropriate
   - In comments: update to reflect new naming
   - In log messages: update to reference new project name
   - In docstrings: update package references

3. Common places to check:
   - Module-level docstrings that reference "update_whitelist"
   - Error messages containing the old name
   - Inline comments

4. After fixing, verify again with the same grep -- must return zero matches.

5. Run the full test suite to confirm nothing broke.

Note: Do NOT update `from Wulihe` -- that is the rule prefix, not the project name, and is configurable per D-01.
  </action>
  <verify>
    <automated>cd /Users/abrahamgreyson/Code/stay-in-whitelist && python -m pytest tests/ -x -q && grep -rn "update.whitelist\|update-whitelist" --include="*.py" stay_in_whitelist/ main.py tests/; test $? -eq 0 || echo "Old branding references remain!"</automated>
  </verify>
  <done>All user-facing strings, log messages, comments, and docstrings updated to reference Stay in Whitelist branding. Zero old project name references in any Python file. Full test suite green.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/ -x -q` -- all tests pass with new import paths
2. `python -c "from stay_in_whitelist.config.config import Config; print('import ok')"` -- package importable
3. `grep -rn "update_whitelist\|update-whitelist" --include="*.py" stay_in_whitelist/ main.py tests/` -- zero matches
4. `grep -n "stay_in_whitelist.log" stay_in_whitelist/logger.py` -- default log filename updated
5. `test -d stay_in_whitelist && ! test -d update_whitelist` -- directory renamed
6. `python -m pytest tests/ -v` -- verbose test output shows all tests passing with new paths
</verification>

<success_criteria>
- Package directory renamed from update_whitelist/ to stay_in_whitelist/ (per D-12)
- All Python imports use stay_in_whitelist (per D-15)
- All test mocker.patch paths use stay_in_whitelist (per D-16)
- Logger default filename is stay_in_whitelist.log (per D-08)
- Config.py comment references stay_in_whitelist directory
- Zero old branding references in string literals, log messages, or comments (IDENT-02)
- Full test suite passes: `python -m pytest tests/ -x -q` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/04-project-identity/04-03-SUMMARY.md`
</output>
