# Create by Abraham Greyson<wechat:abrahamgreyson>
# DateTime 2024/6/23 下午4:10

# 配置文件模板
# 请将此文件复制一份并命名为config.yaml

# 华为云
# huawei:
#   access_key: your_huawei_access_key
#   secret_key: your_huawei_secret_key
#   regions:
#     - region: ap-southeast-2
#       rules:
#         - sg: your_huawei_security_group_id
#           allow:
#             - port: '1-65535'
#               desc: all
#         - sg: your_another_huawei_security_group_id
#           allow:
#             - port: 80
#               desc: http
#             - port: 443
#               desc: https
#             - port: 3306
#               desc: MySQL
#   # Optional: per-provider static IPs
#   # static_ips:
#   #   ips:
#   #     - 203.0.113.50

# 腾讯云
# tencent:
#   access_key: your_tencent_cloud_access_key
#   secret_key: your_tencent_cloud_secret_key
#   regions:
#     - region: ap-bangkok
#       rules:
#         - sg: your_tencent_security_group_id
#           allow:
#             - port: 22
#               desc: ssh
#             - port: 32560
#               desc: MySQL 5 只读节点
#             - port: 32561
#               desc: MySQL 8 只读节点
#             - port: 32562
#               desc: MySQL 5 主节点
#             - port: 32563
#               desc: MySQL 8 主节点
#   # Optional: per-provider static IPs (additional whitelist alongside dynamic IP)
#   # static_ips:
#   #   ips:
#   #     - 203.0.113.50

# 阿里云轻量服务器防火墙
# sg 字段填写轻量服务器实例 ID（非安全组 ID）
# aliyun_firewall:
#   access_key: your_aliyun_access_key
#   secret_key: your_aliyun_secret_key
#   regions:
#     - region: cn-hongkong
#       rules:
#         - sg: your_instance_id
#           allow:
#             - port: 22
#               desc: ssh
#             - port: 32560
#               desc: MySQL 5 只读节点
#             - port: 32561
#               desc: MySQL 8 只读节点
#             - port: 32562
#               desc: MySQL 5 主节点
#             - port: 32563
#               desc: MySQL 8 主节点
#   # static_ips:
#   #   ips:
#   #     - 203.0.113.50
#   #   # Optional: exclude specific ports from static IP rules
#   #   # except_ports:
#   #   #   - 22

# Optional: security group rule description prefix (default: "from Wulihe")
# rule_prefix: "from Wulihe"

# ipinfo
# 请在 ipinfo.io 申请token
# ipinfo:
#   tokens:
#     - your_ipinfo_token_a
#     - your_ipinfo_token_b

# Optional: check interval in seconds (default: 600, minimum: 600)
# check_interval: 600

# Optional: file paths (defaults to same directory as config.yaml)
# paths:
#   ip_cache: /var/lib/stay-in-whitelist/ip_cache.txt
#   log_file: /var/log/stay-in-whitelist/stay_in_whitelist.log

# Optional: timeout settings (defaults shown, most users don't need to change)
# timeouts:
#   ip_detection:
#     connect: 3
#     read: 5
#   cloud_api:
#     connect: 3
#     read: 10
